---
- hosts: all
  gather_facts: true
  become: yes
  tasks:
  - name: ping
    action: ping

  - name: james18
    user:
      name: semaphore
      shell: /bin/bash

  - name: Touch a file
    copy:
      src: ./authorized_keys
      dest: ~/.ssh/authorized_keys
      mode: 400
